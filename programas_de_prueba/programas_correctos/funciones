funciones

begin
    single d;

    /# funcion que calcula 2^p #/
    ulongint fun myFunction (ulongint p)
    begin
        ulongint o;
        d := 5.0;
        x := 2;

        ulongint fun mySubFunction(ulongint g)
        begin
            ret(3);
        end

        if (p = 0) then
            ret(2);
        else
            begin
                ret(2*myFunction(p-1));
            end;
        end_if;
    end

    z, y := 4, myFunction(5);
    d := 3.2s3;

    ulongint fun myFunction2 (single d)
    begin
        ulongint r;
        ret(r);
    end

    single fun myFunction2 (ulongint ñ)
    begin
        ret(3.14);
    end

    d:= 20.1;
    x := 4;
    outf( {Programa terminado } );
end