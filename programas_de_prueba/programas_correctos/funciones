funciones

begin
    single d;
    /# Deberia tirar error por variable sin declarar #/
    e:=2;

    /# funcion que calcula 2^p #/
    ulongint fun myFunction (ulongint p)
    begin
        ulongint o;
        d:=5.0;
        h:= 10;
        x := 2;

        ulongint fun mySubFunction(ulongint g)
        begin
            ret(3);
        end
        if (p = 0) then
            ret(2);
        else
            begin
                ret(2*myFunction(p-1));
            end;
        end_if;
    end
    z, y := 4, myFunction(5);

    ulongint fun myFunction2 (single h)
    begin
        ulongint r;
        r:= mySubFunction(20);
        ret(r);
    end

    single fun myFunction2 (ulongint ñ)
    begin
        ret(3.14);
    end

    o := 50;
    d:= 20.1;
    x := 4;
    outf( {Programa terminado } );
end